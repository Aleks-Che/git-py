"""ViewModel for the commit graph.

Stage 0 stub. Real implementation (parsing commit history into a DAG,
computing the lane-based layout, mapping branches and tags to columns)
lands in Stage 2.
"""
from __future__ import annotations


class GraphViewModel:
    """Placeholder for the Stage 2 graph ViewModel."""

    def __repr__(self) -> str:
        return "GraphViewModel(<stub>)"
