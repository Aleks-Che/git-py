"""ViewModel for the WIP / staging panel.

Stage 0 stub. Real implementation (file list, stage/unstage per file,
commit message, ``staged_files`` property + signal) lands in Stage 3.
"""
from __future__ import annotations


class CommitPanelViewModel:
    """Placeholder for the Stage 3 commit panel ViewModel."""

    def __repr__(self) -> str:
        return "CommitPanelViewModel(<stub>)"
